import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Forward-Deployed AI Engineers | Embedded Teams | Procedure",
  description:
    "Get embedded AI engineers who integrate directly with your team. Senior engineers (8+ years) ship production AI in days. Start in under a week.",
};

export default function ForwardDeployedLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
