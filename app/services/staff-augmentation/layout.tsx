import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Senior AI Engineer Staffing | Contract AI Developers | Procedure",
  description:
    "Hire senior AI engineers with 8+ years experience. No recruiters, no 6-month searches. Contract AI developers ready to start within days.",
};

export default function StaffAugmentationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
